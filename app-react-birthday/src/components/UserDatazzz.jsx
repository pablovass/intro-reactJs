import React, { useState, useEffect } from "react";

const UserData = () => {
return(<div className="fixed inset-0 flex justify-center items-center bg-black bg-opacity-30 backdrop-blur-sm z-50" >hola</div>);
}
export default UserData;
